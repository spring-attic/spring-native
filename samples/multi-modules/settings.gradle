pluginManagement {
	repositories {
		mavenLocal()
		maven { url 'https://repo.spring.io/release' }
		gradlePluginPortal()
		mavenCentral()
	}
}

rootProject.name = 'multi-modules'
include 'core'
include 'app'
